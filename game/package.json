{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve ./src/index.html --no-source-maps",
    "start-https": "parcel serve ./src/index.html --no-source-maps --cert ../serverside/cert.crt --key ../serverside/key.key",
    "deploy": "parcel build ./src/index.html --public-url ./ --dist-dir ./docs --no-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/cli": "^12.0.4",
    "melonjs": "^8.0.1",
    "parcel": "^2.0.0-beta.3.1"
  }
}
